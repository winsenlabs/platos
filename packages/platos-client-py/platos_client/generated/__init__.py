"""Generated V1 SDK surface.

Every module in this package is emitted by ``pnpm generate:sdk-v1``
(``scripts/sdk/v1-contract.mjs``) from the V1 OpenAPI document, the operation
manifest and core-api's idempotency policy. ``pnpm audit:sdk-v1`` regenerates
them and fails when a committed copy differs, so an edit here is reverted by the
next check rather than shipped.
"""
