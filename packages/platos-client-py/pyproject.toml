[project]
name = "platos-client"
version = "0.1.0"
description = "Official Python SDK for Platos — agents, threads, realtime streaming."
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "Platos" }]
license = { text = "Apache-2.0" }
keywords = ["platos", "agents", "llm", "sdk"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: Apache Software License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
]
dependencies = [
  "httpx>=0.27.0",
  "websockets>=13.1",
  "typing-extensions>=4.12.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.3.3",
  "pytest-asyncio>=0.24.0",
  "ruff>=0.7.0",
  "mypy>=1.13.0",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"

[project.urls]
Homepage = "https://platos.dev"
Repository = "https://github.com/platos-dev/platos"
Issues = "https://github.com/platos-dev/platos/issues"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["platos_client"]
