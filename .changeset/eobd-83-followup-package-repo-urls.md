---
"@platos/core": patch
"@platos/redis-worker": patch
---

EOBD.83 follow-up — sweep `repository.url` on every @platos/* package from the inherited `triggerdotdev/trigger.dev` URL to `platos-dev/platos`. No runtime change.
