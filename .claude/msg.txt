WIN-258 T5 (6/n): read the migration to its END — the rating check is IN (-1, 1)

TRAP 1, sprung. 00000000000000_initial installs
MessageRating_rating_check CHECK ("rating" BETWEEN 1 AND 5) at line 2799 and
then, 1,000 lines later IN THE SAME FILE, DROPS it and installs
CHECK ("rating" IN (-1, 1)) — behind a preflight block that refuses to build the
database at all if any existing row holds 2, 3, 4 or 5. The same block adds
`revision` and CHECK ("revision" > 0).

Everything built on the first reading is corrected. The claim that the port's
contract was UNHONOURABLE — that a thumbs-DOWN could not be stored by any
adapter over this schema — was WRONG: the deployed column admits exactly
`domain/rating.ts`'s `RatingValue` and refuses the five-star values the first
constraint allowed. An adapter written against line 2799 alone would have been
wrong in BOTH directions at once, with schema.prisma showing neither constraint.

  RATING_OUTSIDE_SCHEMA_RANGE -> RATING_NOT_THUMBS, refusing anything but 1|-1
  the constraints suite now stores -1 and refuses 3, in that pair
  the conformance scenario flips a rating DOWN, so half the domain's value space
  is exercised rather than asserted unreachable
  M-G46 added: a guard WIDENED past the constraint is as wrong as one narrowed

Also closes the four survivors the first sweep found — M-G18 (append is held to
the three scope refusals too), M-G19 (the isolation flip now uses a storable
value, so only the scope can refuse it), M-G23 (an erasure with a second subject
seeded, whose vote must survive) and M-G27 (an explicit `agentId: undefined`,
the third case `"agentId" in query` exists to tell apart).

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01PzDWyb7YEERRegYTvNwzRp
