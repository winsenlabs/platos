{
  "$schema-note": "Hand-dispositioned. Generated by scripts/arch/secret-response-census.mjs --write, then annotated: the SCANNER supplies path/key/occurrences and the AUTHOR supplies disposition/why. `node scripts/arch/secret-response-census.mjs` fails if the scanner and this file disagree, so a new raw-secret response path cannot land without somebody writing down why it is there. `--write` CARRIES EVERY ANNOTATION THROUGH: it re-reads this file, keeps each surviving site's disposition/why and the whole indirectSites list verbatim, and writes a site it has never seen with null for both -- a shape the check refuses -- so regenerating is safe and cannot launder a new leak into a green manifest.",
  "purpose": [
    "WIN-259 (M2.4) asks for the raw-secret response paths on the oracle to be",
    "FOUND, COUNTED, NAMED, and closed where V1 can reach them. This is the",
    "count, and the finding that matters most is the one it makes obvious:",
    "",
    "  NONE OF THE 29 IS INSIDE V1'S REACH, because V1 has no response surface",
    "  at all. apps/core-api/src/transports/* are seams \u2014 a `kind` and an",
    "  `AppModule` \u2014 and M4 owns the routes. Every occurrence below is in",
    "  apps/agent or apps/webapp, which WIN-258 T6 already put outside M2.",
    "",
    "So the closable work was never 'delete these returns'. It was to make it",
    "structurally impossible for M4's transports to add a twelfth site, which is",
    "what the rest of WIN-259 does: material leaves the vault only as a value",
    "that redacts itself, material ENTERS it only as the same (write-only",
    "inputs), the process logger redacts what it is handed, and a refused read",
    "now leaves evidence. This file is what M4 inherits: eleven rows it must",
    "carry across deliberately rather than rediscover."
  ],
  "method": [
    "A raw-secret response path is a property whose NAME says the value is",
    "material, in an object literal that a REQUEST HANDLER answers with. The",
    "scanner walks the TypeScript AST, not lines, because four things a grep",
    "cannot tell apart from a response all appear in these files: a request",
    "body, a local variable, a JSON schema declaring an input's shape, and a",
    "column read on the way to a store.",
    "",
    "Three precision rules were each added because they changed the number:",
    "  * a handler frame is required \u2014 31 occurrences became 22 once a private",
    "    OAuth config normaliser and an alert-channel normaliser stopped",
    "    counting as responses;",
    "  * `webhookPath: webhookPathRedacted(id)` does not count \u2014 the redacted",
    "    form is the fix, and counting it would push a reader to 'close' the one",
    "    site already closed;",
    "  * a literal assigned to a local that the handler RETURNS does count \u2014",
    "    without that the MCP tool table showed one reveal where it has three,",
    "    reporting the MCP surface as safer than the REST surface it mirrors."
  ],
  "surfaces": 175,
  "totalOccurrences": 37,
  "sites": [
    {
      "path": "apps/agent/src/agent-runtime/agent.controller.ts",
      "key": "token",
      "occurrences": 1,
      "disposition": "m4-transport",
      "why": "`resolveDurableApproval` ECHOES the durable approval token it was called with \u2014 the value is a path segment the caller already holds, not material this call minted, so nothing is revealed that the caller did not send. It is listed because the census counts a credential-shaped field in a response body regardless of where the value came from, which is the right rule: a reader should see the echo and decide, not have the scanner decide for them. The file is `AgentController`, owned by M3.1 (WIN-261); its shape moves with that route."
    },
    {
      "path": "apps/agent/src/agent-runtime/channels.controller.ts",
      "key": "webhookSecret",
      "occurrences": 3,
      "disposition": "one-time-reveal-by-design",
      "why": "Create, BYO-app mint and rotate only. The secret is a segment of the inbound webhook URL the customer must paste into Slack, and it is not readable again from any endpoint. Removing the reveal makes the channel unusable rather than safer. The FILE is a legacy transport (WIN-258 T6, M4), so the shape moves with the route."
    },
    {
      "path": "apps/agent/src/agent-runtime/channels.controller.ts",
      "key": "webhookPath",
      "occurrences": 3,
      "disposition": "one-time-reveal-by-design",
      "why": "The same secret a second time, embedded in a path. It is listed separately because a reader closing `webhookSecret` alone would leave the material in the URL beside it, which is the mistake this row exists to prevent. The list endpoints already answer with webhookPathRedacted."
    },
    {
      "path": "apps/agent/src/agent-runtime/channels.controller.ts",
      "key": "webhookUrl",
      "occurrences": 3,
      "disposition": "one-time-reveal-by-design",
      "why": "The same secret a third time, embedded in an absolute URL. Same reason as webhookPath: the three must be closed together or not at all."
    },
    {
      "path": "apps/agent/src/auth/public-guest-token.controller.ts",
      "key": "token",
      "occurrences": 1,
      "disposition": "one-time-reveal-by-design",
      "why": "`POST /api/v1/public/guest-token` MINTS a guest bearer token and this is its only delivery \u2014 one of the eight operations `apps/core-api/src/http/idempotency-policy.ts` classes `required` for exactly this reason. Removing the reveal makes the guest transport unusable rather than safer. Found by WIN-268 P1, which added the bare key `token` to the material list after building the first V1 route that returns one; before that the census was silent on the most direct spelling of the thing it exists to count."
    },
    {
      "path": "apps/agent/src/auth/session-token.controller.ts",
      "key": "token",
      "occurrences": 1,
      "disposition": "one-time-reveal-by-design",
      "why": "`POST /api/v1/entities/:entityId/session-tokens` MINTS an entity session token and this is its only delivery \u2014 also one of the eight `required` mints. Found by WIN-268 P1 with the same key addition."
    },
    {
      "path": "apps/agent/src/mcp-platform/tools/channels.ts",
      "key": "webhookSecret",
      "occurrences": 3,
      "disposition": "one-time-reveal-by-design",
      "why": "The MCP tool table mirrors the REST controller above: channels.create, channels.mint_from_manifest and channels.rotate_webhook_secret. Two of the three are reached only through a local `const result` that is returned later, which is why the scanner needed the local-return rule to see them."
    },
    {
      "path": "apps/agent/src/mcp-platform/tools/channels.ts",
      "key": "webhookPath",
      "occurrences": 3,
      "disposition": "one-time-reveal-by-design",
      "why": "The mirror of the REST controller's webhookPath, on the same three tools. The other four tools in this file answer with webhookPathRedacted."
    },
    {
      "path": "apps/agent/src/mcp-platform/tools/channels.ts",
      "key": "webhookUrl",
      "occurrences": 3,
      "disposition": "one-time-reveal-by-design",
      "why": "The mirror of the REST controller's webhookUrl, on the same three tools."
    },
    {
      "path": "apps/agent/src/mcp-platform/tools/entities.ts",
      "key": "token",
      "occurrences": 1,
      "disposition": "one-time-reveal-by-design",
      "why": "`entities.generate_mcp_token` returns `minted.raw`, the entity MCP bearer token, once. The MCP tool table is the caller's only chance to read it; the list endpoint beside it returns hashes. Found by WIN-268 P1 with the same key addition."
    },
    {
      "path": "apps/agent/src/mcp-platform/tools/orchestration.ts",
      "key": "plaintextSecret",
      "occurrences": 1,
      "disposition": "one-time-reveal-by-design",
      "why": "The orchestration tool's provisioning step re-serves the entity secret its own registration just minted. It is the only delivery of that secret; the entity cannot read it back, and every later read goes through the vault's runtime-tier path."
    },
    {
      "path": "apps/agent/src/oauth/oauth.controller.ts",
      "key": "access_token",
      "occurrences": 4,
      "disposition": "protocol-required",
      "why": "RFC 6749 section 5.1 makes access_token a REQUIRED member of the token endpoint's success response. A server that redacted it would not be an OAuth server. Four occurrences: the authorization-code and refresh-token branches of both the platform and the entity token endpoints."
    },
    {
      "path": "apps/agent/src/oauth/oauth.controller.ts",
      "key": "refresh_token",
      "occurrences": 4,
      "disposition": "protocol-required",
      "why": "RFC 6749 section 5.1 makes refresh_token an OPTIONAL member of the same response, and this server issues one. Same four branches as access_token, and closable only by dropping refresh-token support."
    },
    {
      "path": "apps/agent/src/test/test.controller.ts",
      "key": "token",
      "occurrences": 3,
      "disposition": "one-time-reveal-by-design",
      "why": "`createSession` mints an entity session token for a test harness and returns it once. The controller is PLATOS_TEST_MODE-only and `apps/agent/src/http/api-surface.ts` lists `test` among the roots that never mount in production, so the exposure is bounded by the mount rather than by the shape. Found by WIN-268 P1 with the same key addition."
    },
    {
      "path": "apps/core-api/src/transports/mcp/token-mint.ts",
      "key": "token",
      "occurrences": 1,
      "disposition": "one-time-reveal-by-design",
      "why": "The V1 MCP token mints' shared response projection. `POST /mcp/platform/tokens` and `POST /mcp/entity/:entityId/tokens` hand back the ONLY copy of the credential they just minted; only its SHA-256 digest is stored, and no read endpoint can return it again. This is why both operations are `required` in the idempotency policy: the `Idempotency-Key` is what makes the retry safe and the recovery possible. The site is visible to this census only because WIN-268 P1 widened `isRequestSurface` to the whole `apps/core-api/src/transports` tree and taught it that a V1 response PROJECTION is a handler \u2014 ADR M0.3 \u00a76's 500-line transport budget forces the literal out of the controller, so a controller-only scan reported this tree as clean no matter what its routes returned."
    },
    {
      "path": "apps/webapp/app/routes/_app.orgs.$organizationSlug.projects.$projectParam.env.$envParam.mcps._index/route.tsx",
      "key": "plaintextSecret",
      "occurrences": 1,
      "disposition": "m4-transport",
      "why": "The create action re-serves the platform MCP bearer token the management transport just minted, once. The metadata half already passes assertCredentialSafePayload; the token half is the reveal. A Remix route is a legacy transport under WIN-258 T6, so the shape moves with M4's surface rather than here."
    },
    {
      "path": "apps/webapp/app/routes/_app.orgs.$organizationSlug.projects.$projectParam.env.$envParam.mcps.$entityId._index/route.tsx",
      "key": "plaintextSecret",
      "occurrences": 1,
      "disposition": "m4-transport",
      "why": "The same reveal for one entity's MCP token rather than the collection. Same disposition and the same owner."
    }
  ],
  "indirectNote": [
    "WHAT THE SCANNER CANNOT SEE, DECLARED RATHER THAN OMITTED.",
    "",
    "The scanner reads syntax, not types. A handler that answers with a value a",
    "SERVICE built \u2014 `return entity`, `return result` \u2014 carries whatever that",
    "service put in it, and no object literal at the handler names the material.",
    "Six such paths were found by hand and are listed below. They are NOT part",
    "of totalOccurrences, because a number that mixes a checked count with a",
    "hand count is neither.",
    "",
    "Each carries an `evidence` string that must still appear in its file. That",
    "is a weaker check than the scanner's \u2014 it proves the line still exists, not",
    "that it still reaches a response \u2014 but it is not nothing: closing the path",
    "removes the string and this file then says so.",
    "",
    "CLOSING THESE NEEDS TYPE INFORMATION, which means a TypeScript Program over",
    "apps/agent rather than a per-file parse. That is a real piece of work and",
    "it is named here rather than pretended away."
  ],
  "indirectSites": [
    {
      "path": "apps/agent/src/auth/auth.service.ts",
      "symbol": "createEntity",
      "evidence": "...(secret ? { plaintextSecret: secret } : {}),",
      "disposition": "one-time-reveal-by-design",
      "why": "The SOURCE of the entity secret reveal. Everything below is a pass-through of this value."
    },
    {
      "path": "apps/agent/src/auth/auth.service.ts",
      "symbol": "regenerateServiceSecret",
      "evidence": "serviceSecret: newSecret,",
      "disposition": "one-time-reveal-by-design",
      "why": "The SOURCE of the entity secret ROTATION reveal, the second half of the same lifecycle."
    },
    {
      "path": "apps/agent/src/agent-runtime/agent.controller.ts",
      "symbol": "registerEntity",
      "evidence": "entity = await this.authService.registerEntity({",
      "disposition": "m4-transport",
      "why": "POST /entities answers with the service value from createEntity, so the plaintextSecret rides out with no literal at the handler naming it."
    },
    {
      "path": "apps/agent/src/agent-runtime/agent.controller.ts",
      "symbol": "regenerateEntitySecret",
      "evidence": "@Post(\"entities/:entityId/regenerate-secret\")",
      "disposition": "m4-transport",
      "why": "POST /entities/:entityId/regenerate-secret answers with the service value from regenerateServiceSecret. Its own comment records the audit finding that made it operator-only: the serviceSecret IS the HMAC key that signs session tokens."
    },
    {
      "path": "apps/agent/src/mcp-platform/tools/entities.ts",
      "symbol": "entities.register",
      "evidence": "const entity = await auth.registerEntity({",
      "disposition": "one-time-reveal-by-design",
      "why": "The MCP mirror of POST /entities. Its own description says the secret is shown once and never again, which is the behaviour, not a claim about it."
    },
    {
      "path": "apps/agent/src/mcp-platform/tools/entities.ts",
      "symbol": "entities.regenerate_secret",
      "evidence": "const result = await auth.regenerateServiceSecret(",
      "disposition": "one-time-reveal-by-design",
      "why": "The MCP mirror of the rotate endpoint. `return result` hands back the whole service value including serviceSecret."
    }
  ]
}
