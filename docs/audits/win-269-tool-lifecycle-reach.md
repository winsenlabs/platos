# WIN-269 (M4.3) — the tool lifecycle's ORM register

Generated by `scripts/arch/tool-lifecycle-reach.mjs`. Do not edit by hand: `pnpm audit:tool-lifecycle-reach` regenerates it and `--check` fails when the tree and this file disagree.

**29 ORM call sites** across 5 of 13 non-test files under `apps/agent/src/tool-gateway` and `apps/core-api/src/transports/tools` — 28 model-delegate calls and 1 client-level reaches.

This register is DISJOINT from WIN-268's by construction: `assertDisjointRoots` compares both root lists prefix-wise in both directions and the build fails on any overlap, so the two counts may be added.

## Verdicts

| verdict | sites | meaning |
| --- | ---: | --- |
| `moved` | 0 | MOVED — the site is already inside apps/core-api/src/transports and reaches a contract |
| `blockedOnContext` | 15 | BLOCKED-ON-CONTEXT — the owning context publishes a contract that the composition root does not compose, so no transport can reach it |
| `blockedOnContract` | 14 | BLOCKED-ON-CONTRACT — the owning context IS composed, and publishes no method for this use case |
| `blockedOnAdapter` | 0 | BLOCKED-ON-ADAPTER — the row is written by the kernel outbox adapter, not by a context |

## Ownership split

| owning context | sites | composed |
| --- | ---: | --- |
| `tools` | 13 | **no** |
| `tenancy` | 10 | yes |
| `agents` | 2 | **no** |
| `secrets` | 2 | yes |
| `<client-level>` | 1 | n/a |
| `identity-access` | 1 | yes |

## What composing `tools` would take, slot by slot

`ToolsDependencies` is read from `packages/contexts/tools/application/dependencies.ts` by AST, so a slot added to the context appears here as a failure rather than as an omission. The context is NOT composed; composing it makes 13 of the 29 sites below reachable from a transport, on top of the 35 WIN-268 measured on the MCP surface.

| slot | kind | satisfied by | note |
| --- | --- | --- | --- |
| `repository` | `adapter` | `postgres-tenancy:ToolsRepository` | a declared, satisfied binding with its own conformance differential in packages/adapters/postgres-tenancy/src/tools-conformance.integration.test.ts. |
| `dispatch` | `root-satisfied` | `packages/contexts/tools/adapters` | the ToolDispatch port, and the ONLY unimplemented thing between this tree and a composed `tools`. ADR M0.3 §5.1 rule (h) (`SDK_CONTAINMENT.mcp-sdk-only-in-tools`) binds `@modelcontextprotocol/*` to `^packages/contexts/tools/(adapters|transport)/` and to nowhere else, so this port cannot be a `packages/adapters/` directory and cannot be a binding row; it is built in the composition root from the factory that home publishes, the way `Judge` is. FOUR CONCRETE STEPS, each measured rather than guessed: (1) the directory does not exist; (2) `gen-v1-skeleton.mjs` OWNS every context package.json `exports` map — it emits `.`, `./application/ports/index.js` and two conditional subpaths — so `./adapters/index.js` needs a THIRD list there beside APPLICATION_ENTRY_PROJECTS and TESTING_ENTRY_PROJECTS, with its own honesty check; (3) the same generator owns runtime dependencies through ADAPTER_RUNTIME_DEPENDENCIES, which is keyed on `packages/adapters/` directories and has no row shape for a context's own adapter, so `@modelcontextprotocol/sdk` has nowhere to be declared yet; (4) `apps/core-api` may import it — `firesCrossContextContractsOnly` requires the FROM side to be a context and an app is not one — so no boundary rule needs changing. WIN-269 landed the prerequisite instead: `DispatchTarget` now names its `transport`, without which the port is not implementable at all. |
| `digest` | `root-satisfied` | `packages/contexts/tools/adapters` | the ContentDigest port — lowercase hex SHA-256 over node:crypto, and the cheap half of the pair. `apps/core-api/src/composition/adapter-bindings.ts` states outright that ContentDigest is `a synchronous host hash with no failure channel and no row`, so it is satisfied where the dispatch is rather than by a binding, and `GOVERNANCE_ROOT_SATISFIED_PORTS = ["Judge"]` is the precedent for a port the composition root fills itself. It needs no SDK and no new list; it is only here because it shares a home with the dispatch. |
| `clock` | `kernel` | `kernel` | the process clock. |
| `ids` | `kernel` | `kernel` | the process id generator. |
| `unitOfWork` | `adapter` | `postgres-tenancy:UnitOfWork` | the same unit of work `secrets` and `providers` are already composed over. |
| `policy` | `domain-default` | `DEFAULT_TOOLS_POLICY` | published from the context's own `.` entry point beside DEFAULT_PROVIDERS_POLICY, which this root already takes. |
| `tenancy` | `peer` | `tenancy (composed)` | composed. |
| `identityAccess` | `peer` | `identity-access (composed)` | composed. |
| `secrets` | `peer` | `secrets (composed)` | composed. |
| `providers` | `peer` | `providers (composed)` | composed. |

## What each file is waiting on

| file | sites | owning contexts | waiting on | published form | what is missing |
| --- | ---: | --- | --- | --- | --- |
| `apps/agent/src/tool-gateway/tool-registry.service.ts` | 12 | `tools`, `tenancy`, `agents`, `<client-level>` | `context-composition` | `tools.registerTools`, `tools.listTools`, `tools.setToolEnabled`, `tools.findTools` | The registry IS `ToolsContract.registerTools` plus its readers. `packages/contexts/tools/application/register-tools.ts` is the published form of the idempotent-replace write, including the prune, and `read-tools.ts` of the two listings. Its `Entity`/`Environment` scope reads are `tenancy.findEntity` and the environment half of the same pair check; the `AgentBinding` fan-out that decides which agents see a tool is `agents`', which is not composed. Blocked on composing `tools`. |
| `apps/agent/src/tool-gateway/tool-executor.service.ts` | 5 | `tools`, `tenancy`, `secrets`, `identity-access` | `context-composition` | `tools.executeTool`, `tools.resolvePermission` | `ToolsContract.executeTool` is this file, and `application/execute-tool.ts` plus `resolve-transport.ts` are the published form of its route resolution, credential substitution and health fold. The `Credential` read is `secrets`' — reached through the vault authorization `ExecuteToolCommand` already carries — and the `McpOidcSession` read is `identity-access`', which publishes no lookup of one. Blocked on composing `tools`. |
| `apps/agent/src/tool-gateway/tool-sync-ws.service.ts` | 4 | `tenancy`, `secrets`, `tools` | `context-composition` | `tools.registerTools` | The `/tools/sync` socket authenticates a wire backend and pushes its declaration into the same `registerTools` write discovery uses, so its registration half is already published. What is NOT published is the socket's own lifecycle: `Entity.connectionStatus` is a liveness fact `tenancy` owns the row for and publishes no writer of, and the `ToolHealth` upsert is `tools`'. Blocked on composing `tools`; the connection-status write needs a `tenancy` method that does not exist. |
| `apps/agent/src/tool-gateway/mcp-transport/entity-mcp-discovery.service.ts` | 7 | `tools`, `tenancy` | `context-composition` | `tools.discoverEntityTools` | `ToolsContract.discoverEntityTools` is this file exactly — same fan-out over the project's environments, same convergence on `registerTools`, same stamp onto the client row. `application/discover-entity-tools.ts`'s own header states the `Entity`-is-project-scoped/`EnvironmentEntityTool`-is-environment-scoped rule this service implements by hand. Blocked on composing `tools`. |
| `apps/agent/src/tool-gateway/mcp-transport/entity-mcp-discovery-scheduler.service.ts` | 1 | `tools` | `context-composition` | `tools.discoverEntityTools` | One `EntityMcpClient.findMany` that selects the stale clients a sweep should re-discover. The sweep ITSELF is `discoverEntityTools` per entity; the selection is not published by `ToolsContract` at all, and a scheduler is a runtime concern rather than a transport, so this file is the one in this root whose destination is a durable job rather than a route. |

## The boundary this register does not cross

| root | owner | why |
| --- | --- | --- |
| `apps/agent/src/agent-runtime` | WIN-261 (M3.1) | AgentController and AgentService serve the entity and tool REST families; M3.1 owns their decomposition and this register would otherwise count its sites twice over two tranches. |

## Every site

| file:line | shape | row | operation | owner | verdict |
| --- | --- | --- | --- | --- | --- |
| `apps/agent/src/tool-gateway/mcp-transport/entity-mcp-discovery-scheduler.service.ts:85` | delegate | `EntityMcpClient` | `findMany` | `tools` | `blockedOnContext` |
| `apps/agent/src/tool-gateway/mcp-transport/entity-mcp-discovery.service.ts:140` | delegate | `Entity` | `findFirst` | `tenancy` | `blockedOnContract` |
| `apps/agent/src/tool-gateway/mcp-transport/entity-mcp-discovery.service.ts:168` | delegate | `Environment` | `findMany` | `tenancy` | `blockedOnContract` |
| `apps/agent/src/tool-gateway/mcp-transport/entity-mcp-discovery.service.ts:422` | delegate | `EntityMcpClient` | `update` | `tools` | `blockedOnContext` |
| `apps/agent/src/tool-gateway/mcp-transport/entity-mcp-discovery.service.ts:428` | delegate | `Entity` | `update` | `tenancy` | `blockedOnContract` |
| `apps/agent/src/tool-gateway/mcp-transport/entity-mcp-discovery.service.ts:455` | delegate | `EntityMcpClient` | `update` | `tools` | `blockedOnContext` |
| `apps/agent/src/tool-gateway/mcp-transport/entity-mcp-discovery.service.ts:465` | delegate | `EntityMcpClient` | `update` | `tools` | `blockedOnContext` |
| `apps/agent/src/tool-gateway/mcp-transport/entity-mcp-discovery.service.ts:475` | delegate | `Entity` | `update` | `tenancy` | `blockedOnContract` |
| `apps/agent/src/tool-gateway/tool-executor.service.ts:909` | delegate | `EnvironmentEntityTool` | `findFirst` | `tools` | `blockedOnContext` |
| `apps/agent/src/tool-gateway/tool-executor.service.ts:968` | delegate | `Entity` | `findFirst` | `tenancy` | `blockedOnContract` |
| `apps/agent/src/tool-gateway/tool-executor.service.ts:1026` | delegate | `Credential` | `findFirst` | `secrets` | `blockedOnContract` |
| `apps/agent/src/tool-gateway/tool-executor.service.ts:1158` | delegate | `McpOidcSession` | `findFirst` | `identity-access` | `blockedOnContract` |
| `apps/agent/src/tool-gateway/tool-executor.service.ts:1598` | delegate | `ToolHealth` | `upsert` | `tools` | `blockedOnContext` |
| `apps/agent/src/tool-gateway/tool-registry.service.ts:161` | delegate | `EnvironmentEntityTool` | `findMany` | `tools` | `blockedOnContext` |
| `apps/agent/src/tool-gateway/tool-registry.service.ts:184` | delegate | `AgentBinding` | `findMany` | `agents` | `blockedOnContext` |
| `apps/agent/src/tool-gateway/tool-registry.service.ts:266` | delegate | `Entity` | `findFirst` | `tenancy` | `blockedOnContract` |
| `apps/agent/src/tool-gateway/tool-registry.service.ts:283` | delegate | `Environment` | `findFirst` | `tenancy` | `blockedOnContract` |
| `apps/agent/src/tool-gateway/tool-registry.service.ts:294` | delegate | `EnvironmentEntityTool` | `findMany` | `tools` | `blockedOnContext` |
| `apps/agent/src/tool-gateway/tool-registry.service.ts:303` | client | — | `$transaction` | `<client-level>` | `blockedOnContract` |
| `apps/agent/src/tool-gateway/tool-registry.service.ts:325` | delegate | `Tool` | `upsert` | `tools` | `blockedOnContext` |
| `apps/agent/src/tool-gateway/tool-registry.service.ts:342` | delegate | `EnvironmentEntityTool` | `upsert` | `tools` | `blockedOnContext` |
| `apps/agent/src/tool-gateway/tool-registry.service.ts:363` | delegate | `EnvironmentEntityTool` | `deleteMany` | `tools` | `blockedOnContext` |
| `apps/agent/src/tool-gateway/tool-registry.service.ts:482` | delegate | `Environment` | `findFirst` | `tenancy` | `blockedOnContract` |
| `apps/agent/src/tool-gateway/tool-registry.service.ts:516` | delegate | `EnvironmentEntityTool` | `update` | `tools` | `blockedOnContext` |
| `apps/agent/src/tool-gateway/tool-registry.service.ts:644` | delegate | `AgentBinding` | `findMany` | `agents` | `blockedOnContext` |
| `apps/agent/src/tool-gateway/tool-sync-ws.service.ts:232` | delegate | `Credential` | `findMany` | `secrets` | `blockedOnContract` |
| `apps/agent/src/tool-gateway/tool-sync-ws.service.ts:341` | delegate | `Entity` | `update` | `tenancy` | `blockedOnContract` |
| `apps/agent/src/tool-gateway/tool-sync-ws.service.ts:385` | delegate | `Entity` | `update` | `tenancy` | `blockedOnContract` |
| `apps/agent/src/tool-gateway/tool-sync-ws.service.ts:640` | delegate | `ToolHealth` | `upsert` | `tools` | `blockedOnContext` |
