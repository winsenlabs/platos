# M0.2 — Capability & Persisted-State Parity Matrix (WIN-247)

> Durable artifact generated by `scripts/capability-matrix.mjs` from source (control-plane manifest + Prisma schemas). This view and `M0.2-capability-matrix.json` are written by the same run, so they cannot diverge. Validate: `pnpm audit:capability-matrix`. Non-vacuous.

## Totals (source-derived)
| Surface | Count |
|---|---|
| REST operations | 322 |
| — from `apps/agent/src` (agent) | 301 |
| — from `apps/core-api/src/transports` (core-api-transports) | 28 |
| operator-protected | 138 |
| MCP tools | 202 |
| Tenancy models | 94 |
| End-user models | 14 |

Source digest `6d42016c7506c680`

## Ownership (WIN-256)

Authority: docs/adr/M0.3-bounded-contexts.md §1. Resolved by `scripts/arch/route-ownership.mjs` against the 89c12b8 oracle.
322 REST rows = 42 resolved from the handler + 280 from the URL prefix. 5 carry `<platform-transport>`, the one non-context value, admitted only for rows that write and read no canonical row.

| Owner | Rows (REST + MCP) |
|---|---|
| `tools` | 229 |
| `tenancy` | 47 |
| `agents` | 39 |
| `conversations` | 31 |
| `cost-monitoring` | 30 |
| `identity-access` | 29 |
| `memory` | 28 |
| `channels` | 22 |
| `governance` | 14 |
| `providers` | 14 |
| `jobs` | 13 |
| `skills` | 12 |
| `<platform-transport>` | 5 |
| `privacy` | 5 |
| `secrets` | 3 |
| `observability` | 2 |
| `files` | 1 |

## Persisted-state gaps (downstream, not fixed in M0)
- **oidc-session-erasure-gap** → WIN-278 (M5.6 — preserve privacy, audit, retention and cryptographic erasure across every store)
- **mcp-anonymous-session-lifecycle** → WIN-278
