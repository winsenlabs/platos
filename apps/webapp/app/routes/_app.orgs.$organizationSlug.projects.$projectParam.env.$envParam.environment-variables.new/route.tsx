import { EnvironmentVariableKind } from "@platos/tenancy-database";
import { json,type ActionFunctionArgs } from "@remix-run/node";
import { Form } from "@remix-run/react";
import { Page } from "~/components/platos/DashboardShell";
import { requireEnvironmentScope } from "~/services/auth.server";
import { database } from "~/services/database.server";
export async function action({request,params}:ActionFunctionArgs){if(!params.organizationSlug||!params.projectParam||!params.envParam)throw new Response("Invalid scope",{status:400});const{scope,operator}=await requireEnvironmentScope({request,organizationSlug:params.organizationSlug,projectSlug:params.projectParam,environmentSlug:params.envParam,access:"secret:mutate"});const form=await request.formData();const key=String(form.get("key")??"").trim();const value=String(form.get("value")??"");if(!/^[A-Z][A-Z0-9_]*$/.test(key))return json({ok:false,error:"Use uppercase environment key syntax"},{status:400});await database.environmentVariable.upsert({where:{environmentId_key:{environmentId:scope.environmentId,key}},create:{environmentId:scope.environmentId,key,kind:EnvironmentVariableKind.PLAIN,value,lastUpdatedBy:operator.userId},update:{kind:EnvironmentVariableKind.PLAIN,value,credentialId:null,lastUpdatedBy:operator.userId,version:{increment:1}}});return json({ok:true});}
export default function NewVariable(){return <Page><Form method="post" className="max-w-xl rounded-lg border border-grid-bright bg-background-bright p-5"><h1 className="text-xl font-semibold">Add plain environment value</h1><p className="mt-2 text-sm text-text-dimmed">Provider secrets are created through Credential-backed provider screens and never redisplayed here.</p><input name="key" placeholder="KEY_NAME" className="mt-5 w-full rounded border border-grid-bright bg-charcoal-900 px-3 py-2"/><textarea name="value" className="mt-3 min-h-28 w-full rounded border border-grid-bright bg-charcoal-900 px-3 py-2"/><button className="mt-3 rounded bg-indigo-500 px-4 py-2">Save</button></Form></Page>;}
