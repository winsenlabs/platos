import { json,type LoaderFunctionArgs } from "@remix-run/node";import{useLoaderData}from"@remix-run/react";import{requireOperator}from"~/services/auth.server";export async function loader({request}:LoaderFunctionArgs){return json({operator:await requireOperator(request)});}export default function AccountIndex(){const{operator}=useLoaderData<typeof loader>();return <main className="grid min-h-screen place-items-center bg-background-dimmed text-text-bright"><div className="rounded-lg border border-grid-bright bg-background-bright p-6"><h1 className="text-xl font-semibold">Operator account</h1><p className="mt-2 text-text-dimmed">{operator.email}</p><code className="mt-4 block text-xs">{operator.userId}</code></div></main>;}
