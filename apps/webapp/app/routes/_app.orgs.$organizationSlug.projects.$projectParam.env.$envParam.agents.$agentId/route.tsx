import { NavLink, Outlet, useParams } from "@remix-run/react";
const tabs=["Config","Context","Skills","Tools","Conversations","Chat","Versions","Canary","Evals A/B","Postman"] as const;
const slugs=["","context","skills","tools","conversations","chat","versions","canary","evals-ab","postman-templates"] as const;
export default function AgentLayout(){const {agentId}=useParams();return <div><div className="border-b border-grid-bright bg-background-bright px-6 pt-4 text-text-bright"><div className="text-sm font-semibold">Agent / {agentId}</div><nav className="mt-3 flex gap-5 overflow-x-auto">{tabs.map((tab,i)=><NavLink end={i===0} key={tab} to={slugs[i]||"."} className={({isActive})=>`border-b-2 pb-2 text-sm ${isActive?"border-indigo-400 text-white":"border-transparent text-text-dimmed"}`}>{tab}</NavLink>)}</nav></div><Outlet/></div>;}
